name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - development

env:
  # Path to the solution file relative to the root of the project.
  SOLUTION_FILE_PATH: ./intermediate/vs2022/ds3os.sln
  BUILD_CONFIGURATION: Release

jobs:
  build:
    name: Build (matrix)
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      matrix:
        include:
          - name: windows-msbuild
            os: windows-latest
            buildType: msbuild
            platform: windows
          - name: linux-make
            os: ubuntu-latest
            buildType: make
            platform: linux
          - name: nix-gcc
            os: ubuntu-latest
            buildType: nix
            nixTarget: .
          - name: nix-clang
            os: ubuntu-latest
            buildType: nix
            nixTarget: .#clang
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Cache build artifacts
      if: matrix.buildType == 'msbuild' || matrix.buildType == 'make'
      uses: actions/cache@v5
      with:
        path: |
          **/intermediate
          ~/.nuget/packages
        key: ${{ runner.os }}-build-${{ hashFiles('**/*.csproj','**/CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-build-

    - name: Generate Solution (Windows)
      if: matrix.buildType == 'msbuild'
      shell: cmd
      run: |
        cd "%GITHUB_WORKSPACE%\Tools"
        "%GITHUB_WORKSPACE%\Tools\generate_vs2022.bat"

    - name: Generate Solution (Linux)
      if: matrix.buildType == 'make'
      shell: bash
      run: |
        cd "${{ github.workspace }}/Tools"
        "${{ github.workspace }}/Tools/generate_make_release.sh"

    - name: Add MSBuild to PATH
      if: matrix.buildType == 'msbuild'
      uses: microsoft/setup-msbuild@v2

    - name: Build on Windows
      if: matrix.buildType == 'msbuild'
      working-directory: ${{github.workspace}}
      run: msbuild /m /p:Configuration=${{env.BUILD_CONFIGURATION}} ${{env.SOLUTION_FILE_PATH}} /t:Restore,Build

    - name: Install uuid development package
      if: matrix.buildType == 'make'
      run: |
        set -euxo pipefail
        sudo apt-get update
        sudo apt-get install -y uuid-dev || sudo apt-get install -y libuuid-dev

    - name: Build on Linux
      if: matrix.buildType == 'make'
      working-directory: ${{github.workspace}}/intermediate/make
      run: make -j$(nproc)

    - name: Install Nix
      if: matrix.buildType == 'nix'
      uses: DeterminateSystems/nix-installer-action@main

    - name: Setup nix cache
      if: matrix.buildType == 'nix'
      uses: DeterminateSystems/magic-nix-cache-action@main
      with:
        use-flakehub: false

    - name: Build with nix
      if: matrix.buildType == 'nix'
      run: nix build -L ${{ matrix.nixTarget }}
